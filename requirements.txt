# --- API ---
fastapi
uvicorn[standard]
pydantic
pydantic-settings
python-multipart
# Session tokens. Passwords use hashlib.scrypt from the standard library, so there is no
# hashing dependency to keep current.
pyjwt

# --- Orchestration ---
# Only the core runtime plus the Foundry client; the `agent-framework` umbrella
# pulls in Bedrock/Gemini/Anthropic/Ollama clients we never use.
agent-framework-core>=1.13.0
agent-framework-foundry>=1.10.4
# Required by MCPStreamableHTTPTool, which reads the Microsoft Learn docs server.
# Pinned below 2: agent-framework 1.13 reads `initialize_result.protocolVersion`, which
# mcp 2.0.0 renamed to `protocol_version`, so connecting raises AttributeError.
mcp>=1.29,<2

# --- Azure ---
openai
azure-identity
azure-search-documents
azure-cosmos
azure-storage-blob
azure-ai-projects

# --- Teams / M365 Agent ---
botbuilder-core
botbuilder-integration-aiohttp
aiohttp

# --- Export ---
markdown
python-docx
weasyprint

# --- Utils ---
httpx
tenacity
structlog
python-dotenv

# --- Dev ---
pytest
pytest-asyncio
ruff
debugpy
agent-dev-cli>=0.0.1b260427
