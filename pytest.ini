[pytest]
testpaths = tests
# Live tests cost money and need Azure, so they are opt-in: `pytest -m live`.
addopts = -m "not live"
markers =
    live: calls the real Foundry model; deselected by default
