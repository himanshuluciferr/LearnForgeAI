"""Agent definitions. Each module builds one ChatAgent with its instructions and tools.

Agent `name` values must be alphanumeric with interior hyphens only (no underscores),
max 63 chars — Foundry rejects anything else.
"""
